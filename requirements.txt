# HySens reference Python environment (Linux x86_64, Python 3.10).
#
# PyTorch is installed from the official CUDA 12.1 wheel index. The CUDA
# runtime is bundled in the wheel; the host only needs a compatible NVIDIA
# driver.

torch==2.2.2+cu121
numpy==1.26.4
scipy==1.12.0
pandas==2.2.1
scikit-learn==1.4.1.post1
rasterio==1.3.9
tifffile==2024.2.12
tqdm==4.66.2
tensorboard==2.16.2
